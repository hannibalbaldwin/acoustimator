# Runtime deps for the Vercel serverless API function.
# Heavy training packages (scikit-learn, xgboost, scipy, pandas, numpy,
# matplotlib, plotly) are intentionally excluded — model .joblib files are
# gitignored and not present on Vercel. The estimator falls back to
# heuristic pricing when ML deps are unavailable.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.0.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29.0
anthropic>=0.40.0
pymupdf>=1.24.0
openpyxl>=3.1.0
rapidfuzz>=3.6.0
httpx>=0.27.0
python-dotenv>=1.0.0
rich>=13.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
